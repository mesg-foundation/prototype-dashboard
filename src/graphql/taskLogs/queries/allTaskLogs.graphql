#import "@/graphql/taskLogs/schema.graphql"

query(
  $eventId: ID!
) {
  allTaskLogs(
    orderBy: createdAt_DESC,
    filter: {
      event: {
        id: $eventId
      }
    }
  ) {
    ...taskLogsFields
  }
}
