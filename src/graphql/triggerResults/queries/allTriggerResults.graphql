#import "@/graphql/triggerResults/schema.graphql"

query(
  $eventId: ID!
) {
  allTriggerResults(
    orderBy: createdAt_DESC,
    filter: {
      event: {
        id: $eventId
      }
    }
  ) {
    ...triggerResultFields
  }
}
