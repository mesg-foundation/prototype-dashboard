fragment eventFields on Event {
  id
  createdAt
  blockId
  transactionId
  executedAt
  payload
  from
  to
  value
  fees
  trigger {
    id
  }
  taskLogs(orderBy: createdAt_ASC) {
    error
  }
}
