#import "@/graphql/events/schema.graphql"

subscription(
  $projectId: ID!
) {
  Event(filter: {
    mutation_in: CREATED,
    node: {
      webhook: {
        project: {
          id: $projectId
        }
      }
    }
  }) {
    mutation
    node {
      ...eventFields
    }
  }
}
