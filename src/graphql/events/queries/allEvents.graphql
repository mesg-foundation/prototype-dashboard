#import "@/graphql/events/schema.graphql"

query(
  $webhookId: ID!
) {
  allEvents(
    orderBy: createdAt_DESC,
    filter: {
      webhook: {
        id: $webhookId
      }
    }
  ) {
    ...eventFields
  }
}
