#import "@/graphql/events/schema.graphql"

query(
  $triggerId: ID!
) {
  allEvents(
    orderBy: createdAt_DESC,
    filter: {
      trigger: {
        id: $triggerId
      }
    }
  ) {
    ...eventFields
  }
}
