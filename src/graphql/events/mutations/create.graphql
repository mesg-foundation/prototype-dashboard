#import "@/graphql/events/schema.graphql"

mutation(
  $triggerId: ID!,
  $payload: Json!,
  $transactionId: String!,
  $blockId: String!,
  $from: String!,
  $to: String!,
  $value: Float!
  $fees: Float!

) {
  createEvent(
    triggerId: $triggerId,
    payload: $payload,
    transactionId: $transactionId,
    blockId: $blockId,
    from: $from,
    to: $to,
    value: $value,
    fees: $fees
	) {
    ...eventFields
  }
}
