#import "@/graphql/events/schema.graphql"

mutation(
  $triggerId: ID!,
  $payload: Json!,
  $transactionId: String!
) {
  createEvent(
    triggerId: $triggerId,
    payload: $payload,
    transactionId: $transactionId
	) {
    ...eventFields
  }
}
