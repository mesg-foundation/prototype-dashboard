#import "@/graphql/actions/schema.graphql"

mutation(
  $id: ID!
  $data: Json!
  $serviceId: ID!
) {
  updateAction(
    id: $id
    data: $data
    serviceId: $serviceId
	) {
    ...actionFields
  }
}
