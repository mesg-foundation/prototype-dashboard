#import "@/graphql/actions/schema.graphql"

mutation(
  $id: ID!
  $data: Json!
  $serviceId: ID!
) {
  createAction(
    id: $id
    data: $data
    serviceId: $serviceId
	) {
    ...actionFields
  }
}
