#import "@/graphql/fragments/webhook.graphql"

subscription(
  $projectId: ID!
) {
  Webhook(filter: {
    mutation_in: UPDATED,
    node: {
      project: {
        id: $projectId
      }
    }
  }) {
    node {
      ...webhookFields
    }
  }
}
