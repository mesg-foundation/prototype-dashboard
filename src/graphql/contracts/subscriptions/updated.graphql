#import "@/graphql/contracts/schema.graphql"

subscription(
  $projectId: ID!
) {
  Contract(filter: {
    mutation_in: [CREATED, UPDATED],
    node: {
      project: {
        id: $projectId
      }
    }
  }) {
    mutation
    node {
      ...contractFields
    }
  }
}
