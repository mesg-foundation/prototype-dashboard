#import "@/graphql/contracts/schema.graphql"

query(
  $projectId: ID!
) {
  allContracts(
    orderBy: createdAt_DESC,
    filter: {
      project: {
        id: $projectId
      }
    }
  ) {
    ...contractFields
  }
}
