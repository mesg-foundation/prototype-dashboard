#import "@/graphql/contracts/schema.graphql"
#import "@/graphql/projects/schema.graphql"

mutation(
  $id: ID!
  $abi: Json!
  $chain: CHAIN
  $public: Boolean
) {
  updateContract(
    id: $id,
    abi: $abi
    chain: $chain
    public: $public
	) {
    ...contractFields
    project {
      ...projectFields
    }
  }
}
