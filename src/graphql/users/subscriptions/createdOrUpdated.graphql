#import "@/graphql/users/schema.graphql"

subscription(
  $projectId: ID!
) {
  User(filter: {
    mutation_in: [CREATED, UPDATED],
    node: {
      project: {
        id: $projectId
      }
    }
  }) {
    node {
      ...userFields
    }
  }
}
