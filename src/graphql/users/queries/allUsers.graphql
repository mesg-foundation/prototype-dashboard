#import "@/graphql/users/schema.graphql"

query(
  $projectId: ID!
) {
  allUsers(
    orderBy: createdAt_DESC,
    filter: {
      projects_some: {
        id: $projectId
      }
    }
  ) {
    ...userFields
  }
}
