#import "@/graphql/contracts/schema.graphql"

fragment webhookFields on Webhook {
  id
  endpoint
  eventName
  authorizationType
  authorizationValue
  secret
  enable,
  createdAt
  contract {
    ...contractFields
  }
}
