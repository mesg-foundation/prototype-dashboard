#import "@/graphql/webhooks/schema.graphql"

query(
  $projectId: ID!
) {
  allWebhooks(
    orderBy: createdAt_DESC,
    filter: {
      project: {
        id: $projectId
      }
    }
  ) {
    ...webhookFields
  }
}
