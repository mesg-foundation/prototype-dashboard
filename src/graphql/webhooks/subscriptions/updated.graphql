#import "@/graphql/webhooks/schema.graphql"

subscription(
  $projectId: ID!
) {
  Webhook(filter: {
    mutation_in: [CREATED, UPDATED],
    node: {
      project: {
        id: $projectId
      }
    }
  }) {
    node {
      ...webhookFields
    }
  }
}
