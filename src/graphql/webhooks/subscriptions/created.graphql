#import "@/graphql/webhooks/schema.graphql"

subscription(
  $projectId: ID!
) {
  Webhook(filter: {
    mutation_in: CREATED,
    node: {
      project: {
        id: $projectId
      }
    }
  }) {
    node {
      ...webhookFields
    }
  }
}
