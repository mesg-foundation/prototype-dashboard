#import "@/graphql/triggers/schema.graphql"

mutation(
  $id: ID!,
  $eventName: String,
  $enable: Boolean,
  $serviceData: Json
) {
  updateTrigger(
    id: $id,
    eventName: $eventName,
    enable: $enable,
    serviceData: $serviceData
	) {
    ...triggerFields
  }
}
