#import "@/graphql/triggers/schema.graphql"

mutation(
  $id: ID!,
  $enable: Boolean,
  $connectorId: ID
  $actionId: ID
) {
  updateTrigger(
    id: $id,
    enable: $enable
    connectorId: $connectorId
    actionId: $actionId
	) {
    ...triggerFields
  }
}
