#import "@/graphql/triggers/schema.graphql"
#import "@/graphql/projects/schema.graphql"

mutation(
  $projectId: ID!
  $connectorId: ID!
  $actionId: ID!
) {
  createTrigger(
    projectId: $projectId,
    connectorId: $connectorId
    actionId: $actionId
	) {
    ...triggerFields
    project {
      ...projectFields
    }
  }
}
