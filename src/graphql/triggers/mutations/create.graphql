#import "@/graphql/triggers/schema.graphql"
#import "@/graphql/projects/schema.graphql"

mutation(
  $projectId: ID!
) {
  createTrigger(
    projectId: $projectId
	) {
    ...triggerFields
    project {
      ...projectFields
    }
  }
}
