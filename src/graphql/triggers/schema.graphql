#import "@/graphql/contracts/schema.graphql"

fragment triggerFields on Trigger {
  id
  eventName
  enable,
  createdAt
  serviceData
  contract {
    ...contractFields
  }
  service {
    id
    name
  }
}
