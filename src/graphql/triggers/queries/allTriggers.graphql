#import "@/graphql/triggers/schema.graphql"

query(
  $projectId: ID!
) {
  allTriggers(
    orderBy: createdAt_DESC,
    filter: {
      project: {
        id: $projectId
      }
    }
  ) {
    ...triggerFields
  }
}
