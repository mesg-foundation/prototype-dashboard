#import "@/graphql/webhookResults/schema.graphql"

subscription(
  $projectId: ID!
) {
  WebhookResult(filter: {
    mutation_in: CREATED,
    node: {
      webhook: {
        project: {
          id: $projectId
        }
      }
    }
  }) {
    node {
      ...webhookResultFields
    }
  }
}
