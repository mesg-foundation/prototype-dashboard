#import "@/graphql/webhookResults/schema.graphql"

query(
  $eventId: ID!
) {
  allWebhookResults(
    orderBy: createdAt_DESC,
    filter: {
      event: {
        id: $eventId
      }
    }
  ) {
    ...webhookResultFields
  }
}
