#import "@/graphql/connectors/schema.graphql"

mutation(
  $id: ID!
  $connectorType: CONNECTOR_TYPE!
  $ethereumContract: EthereumContractConnector
) {
  createConnector(
    id: $id
    connectorType: $connectorType
    ethereumContract: $ethereumContract
	) {
    ...connectorFields
  }
}
