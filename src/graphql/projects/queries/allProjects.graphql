#import "@/graphql/projects/schema.graphql"

query(
  $currentUserId: ID!
) {
  allProjects(
    filter: {
      users_some: {
        id: $currentUserId
      }
    }
  ) {
    ...projectFields
  }
}
