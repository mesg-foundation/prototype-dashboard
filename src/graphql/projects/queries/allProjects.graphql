#import "@/graphql/projects/schema.graphql"

query(
  $currentUserId: ID!
) {
  allProjects(
    orderBy: createdAt_DESC,
    filter: {
      users_some: {
        id: $currentUserId
      }
    }
  ) {
    ...projectFields
  }
}
