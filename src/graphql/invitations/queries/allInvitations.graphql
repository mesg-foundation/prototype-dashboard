#import "@/graphql/invitations/schema.graphql"

query(
  $projectId: ID!
) {
  allInvitations(
    orderBy: createdAt_DESC,
    filter: {
      project: {
        id: $projectId
      }
    }
  ) {
    ...invitationFields
  }
}
